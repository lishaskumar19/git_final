# git_exam
git external

 # 6. To merge between the branches "feature-branch" into master
 --> git checkout -b faeture-branch
 --> git checkout -b master
 --> git merge feature-branch -m "Merging feature-branch into master"
 --> git merge feature-branch --no-ff -e
